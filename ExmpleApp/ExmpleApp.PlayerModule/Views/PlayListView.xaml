<UserControl x:Class="ExmpleApp.PlayerModule.Views.PlayListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExmpleApp.PlayerModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <DataTemplate x:Key="PlayListTemplate">
            <Border Margin="5" BorderThickness="1" BorderBrush="SteelBlue" CornerRadius="4" Width="370">
                <Grid Margin="3">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock FontWeight="Bold" Text="{Binding Artist}"/>
                    <TextBlock Grid.Row="1" Text="{Binding Title}"/>
                    <TextBlock Grid.Row="2" Text="{Binding Url}"/>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid  x:Name="Root" Background="White">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox Name="lstCars" 
                 Margin="5" 
                 ItemTemplate="{StaticResource PlayListTemplate}"
                 ItemsSource="{Binding Music,UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Row="1"
                    Margin="10"
                    Command="{Binding GetPopularMusic}">Neee</Button>
    </Grid>
</UserControl>
