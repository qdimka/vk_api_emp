<UserControl x:Class="ExmpleApp.PlayerModule.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExmpleApp.PlayerModule.Views"
             xmlns:ViewModels="clr-namespace:ExmpleApp.PlayerModule.ViewModels"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="PlayListViewTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid x:Name="controlGrid"
              Grid.Row="0">
            <local:PlayControlView x:Name="playControlView"
                               HorizontalAlignment="Center"
                               DataContext="{Binding PlayerControlViewModel, Mode=OneTime}" />
        </Grid>


        <Grid x:Name="playListGrid"
              Grid.Row="1">
            <ContentControl Margin="5"
                        Style="{StaticResource PlaylistViewStyle}"
                        Content="{Binding PlayListViewModel, Mode=OneTime}" />
        </Grid>
    </Grid>
</UserControl>
