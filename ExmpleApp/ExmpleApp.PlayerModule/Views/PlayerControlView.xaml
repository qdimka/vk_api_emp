<UserControl x:Class="ExmpleApp.PlayerModule.Views.PlayerControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExmpleApp.PlayerModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ContentControl Name="Media" Content="{Binding Media}"/>
        
        
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding CurrentAudio.Title}"/>
            <Slider Minimum="0" Maximum="1" Value="{Binding Volume,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" />
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding PrevAudioCommand}">Prev</Button>
                <Button Command="{Binding PlayPauseCommand}">Play</Button>
                <Button Command="{Binding NextAudioCommand}">Next</Button>
                <Button Command="{Binding StopCommand}">Stop</Button>
            </StackPanel>

        </StackPanel>

    </Grid>
</UserControl>
