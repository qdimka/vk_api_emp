<UserControl x:Class="ExmpleApp.PlayerModule.Views.CurrentItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExmpleApp.PlayerModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding CurrentAudio.Title}"/>
        </StackPanel>
        <Button Command="{Binding PlayCommand}"
                Grid.Row="1" Content="play"></Button>

        <ContentControl Content="{Binding Media}"></ContentControl>
        
    </Grid>
</UserControl>
